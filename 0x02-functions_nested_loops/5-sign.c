#include "holberton.h"
/**
 *
 *
 *
 */

int print_sign(int n);
{
if (n is greater than zero)
{
_putchar('+');
return (0);
}
else if (n is zero)
{
_putchar('0');
return (0);
}
else
{
_putchar(n is less than zero);
return (0);
}
}
