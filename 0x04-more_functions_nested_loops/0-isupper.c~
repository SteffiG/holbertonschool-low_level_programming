#include "holberton.h"

/**
 * isupper - Function that chcks for uppercase character
 * @c: is a parameter
 * Return: 0
 */

int _isupper(int c)
{
	for (c = 65; c <= 90; c++)
	{
		if (c == 65 && c <= 90)
		{
			return (1);
		}
		else
		{
			return (0);
		}
	}
	return (0);
}
